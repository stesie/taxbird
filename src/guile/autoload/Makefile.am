## Process this file with automake to produce Makefile.in

SUBDIRS = 

autoloaddir = $(datadir)/taxbird/guile/autoload
autoload_DATA = \
	sigtest.scm \
	ustva-2005-stub.scm

EXTRA_DIST = $(autoload_DATA) \
	debugging.scm # we don't install this by default, copy it
		      # to one of the autoload/ directories, if you'd
		      # like to have it

